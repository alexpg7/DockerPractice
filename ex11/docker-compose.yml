services:

  app:
    build: ./app
    container_name: ${APP_NAME}
    ports:
      - "${APP_PORT}:80"
    environment:
      DB_HOST: db
      DB_PORT: ${DB_PORT}

  db:
    build: ./db
    container_name: ${DB_NAME}
    volumes:
      - db-data:/data

volumes:
  db-data:
